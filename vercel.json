{
  "version": 2,
  "buildCommand": "if [ -d client ]; then cd client && npm run build && cd .. && mkdir -p dist && cp -R client/dist/. dist/; else npm run build; fi",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
